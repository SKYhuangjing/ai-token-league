# AI Token League 0.5 Baseline

Version: `0.5`
Drafted at: `2026-05-07`
Status: blank baseline. No 0.5.0 product tasks have been opened yet.

This baseline starts from the implemented `0.4-baseline.md`. It exists only as the next-version placeholder and process anchor.

## 1. Baseline State

0.5.0 has not opened any product, engineering, packaging, compatibility, storage, API, privacy, or UI tasks.

The current implemented product state remains `0.4.0`.

## 2. Scope

Current 0.5 scope is empty.

No roadmap candidate is part of 0.5 until it is explicitly admitted into both this baseline and `0.5-development-tasks.md`.

## 3. Temporary / Opportunistic Changes

Temporary or opportunistic code features are also not part of 0.5 by default.

If a temporary feature is admitted later, record it here with:

- behavior
- user value
- boundary
- privacy/storage/API impact
- whether it is permanent, experimental, or deferred
- verification evidence

Current entries:

| ID | Change | Status | Boundary | Verification |
| --- | --- | --- | --- | --- |
| TEMP-001 | Version baseline/task iteration rule documented in `AGENTS.md` | DONE | Documentation process only; no runtime behavior change | `git diff --check`; repository links resolve |

## 4. Verification

For this blank baseline, verification is documentation-only:

```bash
git diff --check
```

When actual 0.5 tasks are opened, add the relevant verification matrix before implementation.

## 5. Rules

- Do not treat roadmap candidates as 0.5 scope until explicitly admitted.
- Do not add implementation tasks without a matching baseline entry.
- Do not mark 0.5 as implemented until code, tests, docs, and release notes are complete.
