# AI Token League 0.5 开发任务

## 0. 范围

0.5.0 当前是空白基线，尚未开启任何产品或工程任务。

当前只记录版本迭代流程本身：后续如果用户说“开 x.x 版本”，必须先建立对应的 baseline 与 development tasks；如果版本中临时加入功能，也必须同时写入 baseline 和任务文档。

## 1. 状态值

```text
TODO        未开始
DOING       开发中
BLOCKED     阻塞中
REVIEW      待评审或待验证
DONE        已完成
DEFERRED    延后，不进入当前版本
```

## 2. 已开启任务

当前没有已开启的 0.5.0 产品任务。

| ID | 任务 | 状态 | 依赖 | 验收标准 |
| --- | --- | --- | --- | --- |
| V0-T1 | 建立 0.5 空白基线 | DONE | 0.4 文档 | `0.5-baseline.md` 明确 0.5.0 尚未开启任务 |
| V0-T2 | 建立 0.5 空白任务文档 | DONE | V0-T1 | 本文档明确没有已开启的 0.5.0 产品任务 |
| V0-T3 | 在 `AGENTS.md` 记录版本迭代规则 | DONE | V0-T1 | 后续“开 x.x 版本”时先建 baseline/task pair；临时功能双文档入账 |

## 3. 未开启候选

以下能力仍属于 roadmap 或后续讨论项，不属于 0.5.0 已开启范围：

- 发布验证闭环
- Release 回滚策略
- 最低支持客户端版本策略
- 本地备份与恢复
- Windows 主机复测
- 客户端诊断数据上报
- 菜单栏程序
- 多语言

## 4. 临时功能入账

当前没有临时加入的 0.5.0 代码功能。

如果后续临时加入功能，必须新增任务行，至少包含：

- 功能行为
- 入账原因
- 影响边界
- 状态
- 验收标准
- 验证结果

当前仅有文档流程项：

| ID | 临时功能 | 状态 | 入账原因 | 验收标准 |
| --- | --- | --- | --- | --- |
| TEMP-001 | AGENTS 记录 baseline/task 迭代规则 | DONE | 用户要求后续“开 x.x 版本”可复用同一开发方式 | `AGENTS.md`、`0.5-baseline.md`、本文档都有对应记录 |

## 5. 验证

当前空白基线只需要文档验证：

```bash
git diff --check
```

后续一旦开启真实 0.5.0 任务，必须先补充对应验证矩阵，再进入实现。
