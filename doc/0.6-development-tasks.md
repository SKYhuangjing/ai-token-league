# AI Token League 0.6 开发任务

## 0. 范围

0.6 已开启五个产品功能：

1. 旧版更新系统遗留清理（详见 `0.6-baseline.md` §2.1）
2. 构建时预置配置系统（详见 `0.6-baseline.md` §2.2）
3. 热更新失败安装包下载兜底（详见 `0.6-baseline.md` §2.3）
4. 社区榜单鉴权与公开首页拆分（详见 `0.6-baseline.md` §2.4）
5. 公开首页宽屏优化与社区聚合预览（详见 `0.6-baseline.md` §2.5）

## 1. 状态值

```text
TODO        未开始
DOING       开发中
BLOCKED     阻塞中
REVIEW      待评审或待验证
DONE        已完成
DEFERRED    延后，不进入当前版本
```

## 2. 已开启任务

### Epic 1: 旧版更新系统遗留清理

| ID | 任务 | 状态 | 依赖 | 关键文件 | 验收标准 |
| --- | --- | --- | --- | --- | --- |
| 0.6-T1 | 删除 update.js 旧版函数 | DEFERRED | — | `src/shared/update.js` | 删除 `buildReleaseManifest()`、`validateReleaseManifest()`、`selectUpdateArtifact()`、`selectInstallerArtifact()`、`updateStateFromManifest()`、`RELEASE_PLATFORMS`、`INSTALLER_PLATFORMS`；保留 `releaseConfigFromEnv()`、`releasePublicConfig()`、`sha256File()`、`sha512Base64()`、`buildLatestYml()` |
| 0.6-T2 | 删除服务端 manifestUrl 字段 | DEFERRED | — | `src/backend/server.js` | `releasePublicConfig()` 响应不再包含 `manifestUrl` 字段 |
| 0.6-T3 | 删除发布脚本 latest.json 上传 | DEFERRED | — | `scripts/publish-release.js` | 移除 `latest.json` 生成和上传；保留 `latest.yml` / `latest-mac.yml` |
| 0.6-T4 | 删除 RELEASE_MANIFEST_PATH 环境变量 | DEFERRED | 0.6-T1 | `env.example`, `src/shared/update.js` | 删除 `RELEASE_MANIFEST_PATH` 相关代码和环境变量 |
| 0.6-T5 | 旧版更新系统延期验证 | DEFERRED | 0.6-T1 ~ 0.6-T4, 0.6-T17, 0.6-T18 | — | 当前版本确认保留 `releaseLatestBody`、`/api/release/latest`、`latest.json`；`/api/release/config` 不暴露 `manifestUrl`；web 下载面板改读 `release.installers`；`npm test` 通过；`npm run desktop:smoke` 通过 |

### Epic 2: 构建时预置配置系统

| ID | 任务 | 状态 | 依赖 | 关键文件 | 验收标准 |
| --- | --- | --- | --- | --- | --- |
| 0.6-T6 | 新建 preset.js 模块 | DONE | — | `src/shared/preset.js` | 导出 `loadBuildPreset(appPath)` 和 `PRESET_ALLOWED_KEYS`；读取 `assets/preset.json`，过滤非白名单字段；文件不存在返回 `{}` |
| 0.6-T7 | 新建 build-preset.js 脚本 | DONE | — | `scripts/build-preset.js` | 从 `PRESET_*` 环境变量读取值，生成 `assets/preset.json`；支持 `--env FILE`；无环境变量时生成 `{}` |
| 0.6-T8 | 修改 ensureDesktopConfig() 注入预置值 | DONE | 0.6-T6 | `src/desktop/main.cjs` | 首次启动时加载 preset，合并到 `initConfig()` 调用；已有配置时不覆盖 |
| 0.6-T9 | npm scripts 和 .gitignore 集成 | DONE | 0.6-T7 | `package.json`, `.gitignore` | 添加 `preset` script；`release:build` 在 package 前执行 preset；`assets/preset.json` 加入 .gitignore |
| 0.6-T10 | 环境变量写入 env.example | DONE | — | `env.example` | 追加 `PRESET_*` 环境变量及注释 |
| 0.6-T11 | 预置配置系统验证 | DONE | 0.6-T6 ~ 0.6-T10 | — | 无预置时行为不变；有预置时首次启动生效；已有配置时不覆盖；`npm test` 通过 |
| 0.6-T12 | 移除 publicUpload 字段 | DONE | — | `src/collector/config.js`, `src/desktop/main.cjs`, `doc/er-diagram.md`, `doc/product-design.md` | 从 config schema、diagnostics、文档中移除未使用的 publicUpload 字段 |
| 0.6-T13 | 语言配置移入 config.json | DONE | — | `src/collector/config.js`, `src/shared/i18n.js`, `src/desktop/renderer.js` | 新增 `language` 字段到 config；i18n 初始化时读取 config.language；语言切换时同步保存到 config.json |
| 0.6-T14 | Provider 预置配置支持 | DONE | 0.6-T7 | `scripts/build-preset.js`, `env.example` | 支持 `PRESET_PROVIDER_<ID>=true/false` 环境变量配置 provider 启用状态 |

### Epic 3: 热更新失败安装包下载兜底

| ID | 任务 | 状态 | 依赖 | 关键文件 | 验收标准 |
| --- | --- | --- | --- | --- | --- |
| 0.6-T15 | 发布脚本写入 installer.json + 服务端透传 | DONE | — | `scripts/publish-release.js`, `src/backend/server.js` | publish-release.js 发布时生成 `installer.json`（含各平台 url/sha256/size/ext）上传到 OSS；服务端 `/api/release/config` 读取该文件，填入 `release.installers` 字段返回；无需运行时推导 |
| 0.6-T16 | 删除 /api/release/latest 端点 | DEFERRED | 0.6-T15 | `src/backend/server.js` | 当前版本保留端点和 `releaseLatestBody()`；待所有活跃客户端迁移出 macOS zip updater 后再删除 |
| 0.6-T17 | Web 下载面板改读 /api/release/config | DONE | 0.6-T15 | `src/web/app.js` | `loadReleaseDownloads()` 改为调用 `/api/release/config`；从 `release.installers` 读取各平台下载地址；渲染逻辑不变（平台选择 + 下载按钮）；错误处理适配新响应结构 |
| 0.6-T18 | 主进程安装包下载 IPC 处理 | DONE | 0.6-T15 | `src/desktop/main.cjs` | 新增 `ipcMain.handle("update:download-installer")`；使用 `session.downloadURL()` 下载安装包到系统临时目录；通过 `update:installer-progress` IPC 推送下载进度；下载完成后校验 SHA-256；调用 `shell.openPath()` 打开安装包 |
| 0.6-T19 | Preload 桥接 + 渲染进程 UI | DONE | 0.6-T18 | `src/desktop/preload.cjs`, `src/desktop/renderer.js`, `src/desktop/index.html`, `src/shared/i18n.js` | preload 暴露 `downloadInstaller` 和 `onInstallerProgress`；Settings > About 面板在热更新失败时展示「下载安装包」按钮；下载中展示进度百分比和速度；下载完成后提示用户完成安装 |
| 0.6-T20 | 安装包下载兜底验证 | REVIEW | 0.6-T15 ~ 0.6-T19 | — | `/api/release/config` 可返回 `release.installers` 三平台元数据；web 下载面板从 config 渲染；桌面热更新失败后展示安装包下载入口；`npm test` 通过；`npm run desktop:smoke` 通过 |

### Epic 4: 社区榜单鉴权与公开首页拆分

| ID | 任务 | 状态 | 依赖 | 关键文件 | 验收标准 |
| --- | --- | --- | --- | --- | --- |
| 0.6-T21 | 榜单 BasicAuth 边界设计入账 | DONE | — | `doc/0.6-baseline.md`, `doc/0.6-development-tasks.md` | 产研方案明确保护范围、公开范围、默认配置、排除方案和验证矩阵 |
| 0.6-T22 | 服务端增加榜单鉴权配置 | DONE | 0.6-T21 | `src/backend/server.js`, `env.example`, `doc/operations.md` | 新增 `PUBLIC_BOARD_AUTH_USERNAME` / `PUBLIC_BOARD_AUTH_PASSWORD`；榜单鉴权与 admin 独立；仅配置 admin 时榜单仍公开；显式配置同值时才共用账号 |
| 0.6-T23 | 统一榜单 API 命名空间和鉴权入口 | DONE | 0.6-T22 | `src/backend/server.js`, `tests/` | 新增 `/api/board/*`；`handleApi()` 入口像 `/api/admin/*` 一样统一校验 board BasicAuth；目标接口为 `/api/board/leaderboard`、`/api/board/participants/:id`、`/api/board/participants/:id/trend` |
| 0.6-T24 | 移除旧榜单 API | DONE | 0.6-T23 | `src/backend/server.js`, `tests/` | 不保留兼容别名；`/api/public-leaderboard`、`/api/participants/:id`、`/api/participants/:id/trend` 返回 `404` |
| 0.6-T25 | 首页改为公开下载页 | DONE | 0.6-T21 | `src/web/download.html`, `src/web/download.js`, `src/web/styles.css`, `src/shared/i18n.js` | `/` 为公开首页；展示产品简介、版本信息、功能特性、下载按钮、榜单入口链接和语言切换；读取 `/api/release/config`；中英文文案齐全 |
| 0.6-T26 | 榜单页独立并迁移前端 API | DONE | 0.6-T23, 0.6-T25 | `src/web/leaderboard.html`, `src/web/leaderboard.js`, `src/web/styles.css` | 榜单页迁移到 `/leaderboard.html`（受 board auth 保护）；`index.html` 和 `app.js` 重命名；前端改读 `/api/board/*`；榜单周期、成本开关、参与者详情不回归 |
| 0.6-T27 | 文档入口与部署说明更新 | DONE | 0.6-T22 ~ 0.6-T26 | `README.md`, `README.en.md`, `doc/README.md`, `doc/operations.md`, `doc/smoke-checklist.md` | 公开入口为 `/`；榜单入口为 `/leaderboard.html`；榜单鉴权环境变量、`/api/board/*` 和旧接口 `404` curl 验证步骤可执行 |
| 0.6-T28 | 榜单鉴权与首页验证 | DONE | 0.6-T22 ~ 0.6-T27 | — | 完成未鉴权兼容、仅 admin 鉴权时榜单公开、独立 board 凭据、显式同值共用凭据、旧接口 `404`、公开首页展示功能特性和下载、榜单页独立且受鉴权保护、`node --check`、`npm test` 验证 |

### Epic 5: 公开首页宽屏优化与社区聚合预览

| ID | 任务 | 状态 | 依赖 | 关键文件 | 验收标准 |
| --- | --- | --- | --- | --- | --- |
| 0.6-T29 | 首页信息架构基线确认 | REVIEW | — | `doc/0.6-baseline.md`, `doc/0.6-development-tasks.md` | 确认采用上下布局：顶部产品名，中上社区聚合预览，中下下载区，底部功能特性；不采用双栏 hero；不保留重复双 CTA；确认公开聚合指标是否允许展示 |
| 0.6-T30 | 公开聚合预览数据源 | REVIEW | 0.6-T29 | `src/backend/server.js`, `tests/` | 若允许公开聚合指标，新增公开 summary 数据源，只返回参与人数、今日总 token、本周总 token；不得返回 participant id、nickname、models、sources、workdirs、成本或明细；开启 board auth 后 summary 仍可公开访问，完整榜单仍受保护 |
| 0.6-T31 | 首页上下布局改造 | TODO | 0.6-T29 | `src/web/download.html`, `src/web/styles.css` | PC 宽屏首屏展示产品名、聚合预览、排行榜入口和三平台下载卡；容器不再受 `max-width: 720px` 窄栏限制；移动端单列无横向滚动 |
| 0.6-T32 | 下载页渲染与 i18n 补齐 | TODO | 0.6-T30, 0.6-T31 | `src/web/download.js`, `src/shared/i18n.js` | 渲染当前参与人数、今日总 token、本周总 token；所有新增用户可见文案支持 `zh-CN` 和 `en`；数据加载失败时只降级预览模块，不影响下载卡 |
| 0.6-T33 | 首页宽屏与移动端验收 | TODO | 0.6-T31, 0.6-T32 | — | 1680x900 和 1440x900 首屏无需滚动即可看到排行榜入口和三平台下载卡；390px 移动端无重叠和横向滚动；`node --check src/web/download.js`、必要时 `node --check src/backend/server.js`、`npm test` 通过 |

## 3. 验证

```bash
node --check scripts/build-preset.js
node --check scripts/publish-release.js
node --check src/desktop/main.cjs
node --check src/backend/server.js
node --check src/web/leaderboard.js
node --check src/web/download.js
npm test
npm run desktop:smoke
npm run release:build
```

## 4. 临时功能入账

当前无临时功能。
